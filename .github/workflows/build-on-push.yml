name: Build on push

on:
    push:
        branches:
            - "develop"
            - "master"
            - "support/**"

jobs:
    call-build-image:
        uses: ./.github/actions/build-image.yml
        with:
            ref: ${{ github.ref }}
